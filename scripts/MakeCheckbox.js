const makeCheckboxes = () => {
  showclockscheckbox = createCheckbox('Show Conveyor Belt Clocks')
  showclockscheckbox.position(40,20)
  showdistancecheckbox = createCheckbox('Show Conveyor Belt Distance Markers')
  showdistancecheckbox.position(40,40)
  showvectorscheckbox = createCheckbox('Show Velocity Vectors')
  showvectorscheckbox.position(40,360)
}
